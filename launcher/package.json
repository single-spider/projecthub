{
  "name": "projecthub-radial-launcher",
  "private": true,
  "version": "0.2.0",
  "description": "High-fidelity mechanical radial launcher prototype for ProjectHub.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "capture": "electron . --capture-test",
    "test:tree": "node tests/tree.test.js",
    "test": "npm run test:syntax && npm run test:tree && npm run test:visual",
    "test:syntax": "node --check renderer.js && node --check main.js && node --check mock-data.js && node --check tree.js",
    "test:visual": "node tests/visual-structure.test.js"
  },
  "devDependencies": {
    "electron": "^36.0.0"
  }
}
